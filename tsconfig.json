{
  "compilerOptions": {
    "target": "es6",               // Ensure the target is ES6 or later
    "lib": ["es2015", "dom"],      // Include ES6 features and DOM APIs
    "strict": true,                // Optional: Enable strict type-checking
    "esModuleInterop": true        // Optional: Allow compatibility with non-ES modules
  }
}